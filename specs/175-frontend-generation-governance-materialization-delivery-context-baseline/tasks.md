# 任务清单：Frontend Generation Governance Materialization Delivery Context Baseline

- [x] 为 generation constraints 增加 `page_schema_ids`
- [x] generation handoff 返回并显示 `page schema ids`
- [x] `generation.manifest.yaml` 保留 `page_schema_ids`
- [x] `rules materialize-frontend-mvp` 绑定当前项目 delivery context
- [x] remediation governance materialization 绑定同一条 resolved generation constraints truth
- [x] 补测试与 formal docs
