# 任务清单：[FEATURE_NAME]

**输入**：来自 `/specs/NNN-feature-name/` 的设计文档
**前置条件**：plan.md、spec.md、research.md、data-model.md、quickstart.md、contracts/

**可选元数据（YAML frontmatter，若仓库支持）**：

```yaml
---
related_plan: ".cursor/plans/your-plan-name.plan.md"   # 或相对本仓库的其它计划文件路径；无则省略
---
```

- **`related_plan`**：指向本工作项依赖的**外部实施计划**（各 IDE 路径不同），便于人工与将来 **plan-check** 对账；**非必填**。与 **FR-086** 一致。

**测试要求**：[是否必须编写测试，依据宪章和 spec 判断]

**组织方式**：任务按用户故事分组，以支持每个故事独立实现、独立测试。

## 格式：`[ID] [P?] [Story] 描述`

- **[P]**：可并行执行（不同文件、无依赖）
- **[Story]**：该任务所属用户故事（例如 `US1`、`US2`）
- 描述中必须包含精确文件路径

## 路径约定

- [根据项目实际结构填充]

## Phase 1：准备阶段

**目的**：补齐缺失的设计产物，并建立最小仓库骨架。

- [ ] T001 [描述 + 文件路径]
- [ ] T002 [P] [描述 + 文件路径]

---

## Phase 2：基础阶段（阻塞性前置条件）

**目的**：搭建任何用户故事开始前必须完成的核心基础设施

- [ ] T0XX [描述 + 文件路径]

**检查点**：基础设施就绪，后续用户故事可以开始实现

---

## Phase 3：用户故事 1 - [US1 标题]（优先级：P?）

**目标**：[一句话]
**独立测试**：[如何验证]

### US1 的测试

- [ ] T0XX [P] [US1] [测试描述 + 文件路径]

### US1 的实现

- [ ] T0XX [US1] [实现描述 + 文件路径]

**检查点**：US1 应可独立运行并独立测试

---

## 任务块必须包含「可验收」字段（DECOMPOSE 门禁约束）

为避免「可规划」被误当成「可直接执行」，每个任务块必须包含至少一类可验收字段（满足其一即可）：

- **验收标准（AC）**：可被验证的条件列表
- **验证**：明确的验证命令（如 `uv run pytest ...` / `uv run ruff check ...`）

若缺失上述字段，后续将由 DECOMPOSE Gate（FR-090）判定为不通过。

---

## Phase N+1：收尾与跨切面关注点

- [ ] T0XX [P] [收尾任务描述 + 文件路径]

---

## 依赖与执行顺序

### 阶段依赖

- Phase 1 → Phase 2 → Phase 3+ → 收尾

### 可并行机会

- [列出]

---

## 实施策略

### MVP 优先

1. 完成 Phase 1 和 Phase 2
2. 完成 US1
3. 停下来验证

### 渐进式交付

1. 基础设施就绪
2. 交付 US1 并验证
3. 交付 US2 并验证
4. ...
