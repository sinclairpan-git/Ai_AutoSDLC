# 功能规格：{{ project_name }}

**功能编号**：`{{ work_item_id }}`
**创建日期**：{{ created_at }}
**状态**：草稿

---

## 目标

{{ goals | default('TODO: 填写功能目标') }}

## 范围

### 覆盖

{{ scope_in | default('TODO: 填写覆盖范围') }}

### 不覆盖

{{ scope_out | default('TODO: 填写不覆盖范围') }}

---

## 用户场景与测试

TODO: 定义用户故事和验收场景，例如：
### 用户故事 1
场景 1: 描述一个可验证的验收场景
说明：`场景 1:` / `Scenario 1:` 为推荐写法，`**场景 1**`、`#### 场景 1`、`- 场景 1` 也视为合法场景标题。

---

## 功能需求

TODO: 定义 FR-xxx 功能需求列表

---

## ROI 与实现边界（实现前必填）

1. **用户可观察收益或可复现风险**：[不能只写“减少 LOC”或“更优雅”]
2. **现状证据**：[命令、失败样例、真实重复路径或兼容合同]
3. **最小方案 / 备选方案**：[说明为什么不采用更小或现有承载方式]
4. **总投入**：[估算产品、测试/harness、CI、评审、迁移和长期维护]
5. **范围与退出条件**：[说明新增 public API、依赖、持久化状态，以及回退/删除触发器]
6. **决策**：`implement` / `defer` / `needs_user` / `not-applicable`；[理由]

微小修复或不适用事项可只写一行 `not-applicable` 理由，不要求评分表。
`400/50`、辅助代码大于核心、少于三个调用方的公共抽象仅是风险信号，不单独构成 blocker。
只有未经授权的范围扩展、缺失可执行证据，或可复现的安全、隐私、数据、兼容、回归问题可以成为 blocker。
安全、兼容、迁移、恢复或外部协议实现可以合理超过一般支撑比例，但必须说明必要性和退出条件。

---

## 验收标准

{{ acceptance_criteria | default('TODO: 定义可量化的验收标准') }}
