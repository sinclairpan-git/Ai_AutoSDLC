work_item_id: "{{ work_item_id }}"
work_type: "{{ work_type }}"
severity: "{{ severity }}"
source: "{{ source }}"
status: "{{ status }}"
title: "{{ title }}"
description: "{{ description }}"
created_at: "{{ created_at }}"
updated_at: "{{ updated_at }}"
needs_human_confirmation: {{ needs_human_confirmation | lower }}
classification_confidence: "{{ classification_confidence }}"
